# Local dev — no Firebase required when USE_MEMORY_DB=1
USE_MEMORY_DB=1
NODE_ENV=development
PORT=3005

# Optional — forensic + hunt AI scoring (falls back to defaults if missing)
GROQ_API_KEY=

# Frontend wallet connect (optional for UI)
VITE_REOWN_PROJECT_ID=e4f0acb8bbf35146eb2bce8c7006d1c3

# Explorer — improves hunt target txSamples
ETHERSCAN_API_KEY=
